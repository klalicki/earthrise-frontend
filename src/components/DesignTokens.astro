---
import "@acab/reset.css";
import "@fontsource-variable/lexend";
import "@fontsource/poppins";
import "./../../design-tokens/tokens.css";
---

<style is:global>
  :where(.theme-dark, .theme-light) {
    background-color: var(--color-local-background);
    color: var(--color-local-text);
  }
  :root {
    /* theme color palettes: semantic */

    --gradient-stepped-rainbow: linear-gradient(
      90deg,
      #fd5d36 0%,
      #fd5d36 16.6%,
      #ffb155 16.6%,
      #ffb155 33.3%,
      #f2ff63 33.3%,
      #f2ff63 50%,
      #5fe5ad 50%,
      #5fe5ad 66.6%,
      #81c1ff 66.6%,
      #81c1ff 83.3%,
      #e6b2e4 83.3%,
      #e6b2e4 100%
    );
    --font-main: "Lexend Variable", sans-serif;
    --font-headline: "Poppins", sans-serif;
    --size-page-width: 1200px;
    --size-padded-page: min(var(--size-page-width), calc(100vw - 2rem));

    --size-content-width: 960px;
    --size-padded-content: min(var(--size-content-width), calc(100vw - 2rem));
    --size-super-width: 1440px;
    --size-padded-super: min(var(--size-super-width), calc(100vw - 2rem));

    --size-page-padding: 1rem;
    --font-size-navitems: 1rem;
    --font-size-body: 1.25rem;

    font-family: var(--font-main);
    font-weight: 300;
    color: var(--color-local-text);
    background-color: var(--color-local-background);

    font-size: 16px;
  }

  input {
    background-color: var(--color-local-background);
  }
  button {
    background-color: var(--color-local-cta-background);
    color: var(--color-local-cta-text);
  }
  p,
  dl,
  dd,
  dt,
  li {
    font-size: 1.25rem;
  }
  * {
    font-family: var(--font-main);
  }
  h1 {
    font-size: 3rem;
    font-family: var(--font-headline);
  }
  h2 {
    font-size: 2rem;
    font-weight: 500;
  }
  h3 {
    font-size: 1.5rem;
    font-weight: 500;
  }
  h4 {
    font-size: 1.25rem;
    font-weight: 500;
  }

  .text-block > * {
    margin-top: 1em;
    margin-bottom: 1em;
  }
  .text-block > *:last-child {
    margin-bottom: 3rem;
  }
  .text-block-tight > *:last-child {
    margin-bottom: 0;
  }
  .text-block-tight > *:first-child {
    margin-top: 0;
  }
  ul,
  ol {
    padding-left: 1.5rem;
  }
  ul {
    list-style: disc outside;
  }
  ol {
    list-style: decimal;
  }

  dt {
    font-size: 1.25rem;
    font-weight: 600;
  }
  dd {
    padding-left: 1rem;
    margin-bottom: 1rem;
  }

  .no-margin-after {
    margin-bottom: 0 !important;
  }
  .no-margin-before {
    margin-top: 0 !important;
  }
</style>
<style is:global>
  .ec-button {
    padding: 0.75rem 1.5rem;
    border-radius: 6px;
    background-color: var(--color-local-cta-background);
    border: 2px solid var(--color-local-cta-background);
    color: var(--color-local-cta-text);
    text-decoration: none;
    display: inline-flex;
    gap: 0.5em;
    font-weight: 500;
  }
  .ec-button:hover,
  .ec-button:focus {
    background-color: var(--color-local-cta-text);
    color: var(--color-local-cta);
  }

  .theme-dark {
    /* --color-local-background: var(--color-dark-background);
    --color-local-text: var(--color-dark-text);
    --color-local-cta: var(--color-orange-500);
    --color-local-cta-text: var(--color-dark-background);
    --color-local-border: var(--color-dark-border); */
    font-weight: 300;
  }
  .theme-light {
    font-weight: 300;
    /* --color-local-background: var(--color-light-background);
    --color-local-text: var(--color-light-text);
    --color-local-cta: var(--color-blue-500);
    --color-local-cta-text: var(--color-light-background);
    --color-local-border: var(--color-light-border); */
  }
  /*   * {
    border: 1px solid red !important;
  } */
</style>

<style is:global>
  /* Table Styles */
  table {
    color: var(--color-text);
    border-collapse: collapse;
    border-radius: 0.5rem;
  }
  table caption {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
  }
  td,
  th {
    border: 1px solid var(--color-local-border);
    padding: 1.5rem;
  }
  thead {
    background-color: var(--color-theme-accent-a);
    color: var(--color-theme-background);
    border-radius: 0.5rem 0.5rem 0 0;
  }

  /* Utility Class to create a plain text container (decent for Markdown, etc) */

  .text-container > * {
    margin-top: 0.5em;
    margin-bottom: 0.125em;
  }
  .text-container > *:first-child {
    margin-top: 0;
  }
  .text-container > *:last-child {
    margin-bottom: 0;
  }

  .text-container > p:has(img) {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    place-items: center;
    margin-bottom: 2rem;
    margin-top: 2rem;
    max-width: calc(100% - 4rem);
    margin-left: auto;
    margin-right: auto;
  }
  .text-container img {
  }
  .text-container > p:has(img) br {
    display: none;
  }
</style>
